apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tracking-service.fullname" . }}-test-connection"
  labels:
    {{- include "tracking-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tracking-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
